# AeroScientia — Level 1: NLP Pipeline
# Install: pip install -r requirements.txt
# Then:    python -m spacy download en_core_web_trf

# ── Core NLP ──────────────────────────────────────────
spacy>=3.7.0
spacy-transformers>=1.3.0       # For transformer-based NER
en-core-web-trf                 # Base model (download separately)

# ── HuggingFace ───────────────────────────────────────
transformers>=4.40.0
sentence-transformers>=2.7.0    # Semantic embeddings
datasets>=2.19.0                # Dataset management

# ── Data Processing ───────────────────────────────────
streamlit>=1.32.0
pandas>=2.2.0
numpy>=1.26.0
plotly>=5.22.0
spacy>=3.7.0
pdfplumber>=0.11.0              # PDF text extraction
tqdm>=4.66.0                    # Progress bars
pymupdf>=1.24.0                 # Fitz — alternative PDF reader
               
# ── Bilingual / Translation ───────────────────────────
langdetect>=1.0.9               # Language detection
deep-translator>=1.11.4         # Translation fallback

# ── Visualization & Reporting ─────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.22.0
rich>=13.7.0                    # Beautiful terminal output

# ── Evaluation ────────────────────────────────────────
scikit-learn>=1.4.0             # Classification metrics
wasabi>=1.1.0                   # spaCy's pretty printer

# ── Export ────────────────────────────────────────────
openpyxl>=3.1.0                 # Excel export
jinja2>=3.1.0                   # HTML report templates
